Use the knowledge-compiler skill at `.agents/skills/knowledge-compiler` to edit exactly this one concept file: `<path>`.

Preserve `id`, `type`, and `title` frontmatter. Keep source in token-efficient English, use YAML fences for structured data, avoid Markdown tables, and run compile after editing. Stop if the task requires multiple files or schema changes.
